[project]
name = "model-router-agent"
version = "0.0.0"
description = "A Managed Deep Agent project."
requires-python = ">=3.11"
dependencies = [
    "managed-deepagents==0.7.1.dev3",
    "langchain>=1.4.0",
    "langchain-openai>=1.6.2",
    "httpx>=0.27",
    # `ModelRouterMiddleware` and `AutoModeMiddleware` both live behind the
    # `experimental` extra. Pinned to an exact prerelease because the namespace
    # is documented as changing without notice.
    "langchain-typesafe[experimental]==0.0.1a2",
]
